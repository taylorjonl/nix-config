# Hosts

All hosts have a ZFS structure like this:

```
  tank
  ├── impermanent
  │   ├── nix
  ├── persistent
  │   ├── home
  │   │   ├── root
  │   │   └── jltaylor
  │   └── persist
  └── transient
      └── rootfs
```
